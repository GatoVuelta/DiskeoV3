/// @description check if we can use the avatar.
//if (!global.hassprite)
//{
if (async_load[? "status"] >= 0) {
	if (async_load[? "id"] = spr_id)
	{
		asset_sprite_this = async_load[? "id"];
		asset_sprite_exists = true;
		
		//sprite_set_offset(asset_sprite_this, sprite_get_width(asset_sprite_this)/2, sprite_get_height(asset_sprite_this)/2);
	}
}
//}